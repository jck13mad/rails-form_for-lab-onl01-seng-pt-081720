<h1>Student Form</h1>

<%= form_for(@student) do |s| %>
  <%= s.first_name %>: <%= s.text_field :first_name %><br>
  
  <%= s.last_name %>: <%= s.text_field :last_name %><br>
  
  <%= s.submit %>
<% end %>